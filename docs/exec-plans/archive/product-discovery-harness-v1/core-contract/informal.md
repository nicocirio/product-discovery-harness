# Informal source — Core contract

Implement the package foundation required by the build specification: target
mode detection, preservation-safe bootstrap, complete target templates and JSON
schemas, stable IDs, record lifecycle rules, and CI-suitable validation. The
target repository is durable memory; code is evidence, not future truth.
