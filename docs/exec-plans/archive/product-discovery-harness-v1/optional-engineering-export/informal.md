# Informal source — Optional engineering export

Make `docs/product-specs/<feature>.md` the canonical Product Discovery Harness
output. Engineering Harness `informal.md` becomes an explicit, generated export
only. Product targets must work without `docs/exec-plans`; existing exports and
engineering artifacts must be preserved and never overwritten silently.
