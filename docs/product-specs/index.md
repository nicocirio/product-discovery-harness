# Product Specs Index

This directory is the canonical, product-owned source of truth for accepted
feature specs. Any `docs/exec-plans/.../informal.md` is an optional compatibility
export and never replaces a product spec.
